<UserControl x:Class="MorphFaceEditor.Controls.BoneTransformControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MorphFaceEditor.Controls"
             Focusable="False"
             HorizontalAlignment="Stretch">
    <Grid SizeChanged="OnControlSizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="26" />
        </Grid.RowDefinitions>

        <controls:SquareDecorator HorizontalAlignment="Stretch">
            <Border x:Name="Pad"
                    Background="#151A20"
                    BorderBrush="#53606D"
                    BorderThickness="1"
                    Cursor="Cross"
                    MouseLeftButtonDown="OnPadMouseLeftButtonDown"
                    MouseMove="OnPadMouseMove"
                    MouseLeftButtonUp="OnPadMouseLeftButtonUp"
                    LostMouseCapture="OnPadLostMouseCapture"
                    PreviewMouseWheel="OnPadMouseWheel">
                <Grid Margin="1" ClipToBounds="True">
                    <Rectangle Width="1" HorizontalAlignment="Center" Fill="#38434F" />
                    <Rectangle Height="1" VerticalAlignment="Center" Fill="#38434F" />
                    <TextBlock Text="Up (Z+)" Foreground="{StaticResource MutedText}" FontSize="9"
                               HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0" />
                    <TextBlock Text="Down (Z-)" Foreground="{StaticResource MutedText}" FontSize="9"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5" />
                    <TextBlock Text="Left (Y-)" Foreground="{StaticResource MutedText}" FontSize="9"
                               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" />
                    <TextBlock Text="Right (Y+)" Foreground="{StaticResource MutedText}" FontSize="9"
                               HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" />
                    <Canvas x:Name="PuckCanvas" IsHitTestVisible="False">
                        <Ellipse x:Name="Puck"
                                 Width="20" Height="20"
                                 Fill="{StaticResource Accent}"
                                 Stroke="#E7FAFF"
                                 StrokeThickness="2" />
                    </Canvas>
                </Grid>
            </Border>
        </controls:SquareDecorator>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Depth (X +/-)" Foreground="{StaticResource SecondaryText}" FontSize="10"
                       Margin="0,0,8,0" VerticalAlignment="Center" />
            <Grid x:Name="DepthTrack" Grid.Column="1" Height="18">
                <Rectangle Height="2" Fill="#46525F" VerticalAlignment="Center" />
                <Rectangle Width="1" Height="10" Fill="{StaticResource MutedText}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Canvas x:Name="DepthCanvas" IsHitTestVisible="False">
                    <Rectangle x:Name="DepthThumb" Canvas.Top="3" Width="3" Height="12" Fill="{StaticResource Accent}" />
                </Canvas>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
